[tool.poetry]
name = "multi-dimpy"
version = "0.1.0"
description = ""
authors = ["Nathan Schill <nateschill@gmail.com>"]
readme = "README.md"
packages = [{include = "dimpy", from = "src"}]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^2.0.1"

[tool.poetry.group.dev.dependencies]
ruff = "^0.5.4"
pytest = "^8.3.1"
pytest-cov = "^5.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
